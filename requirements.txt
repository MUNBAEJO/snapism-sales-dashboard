streamlit>=1.32.0,<1.46.0
pandas>=2.0.0
plotly>=5.18.0
openpyxl>=3.1.0
playwright>=1.40.0
# Google 로그인(st.login)용. 1.6+ 는 Streamlit 1.45 OIDC 연동과 비호환
# (set_state_data 가 session=None 에 쓰려다 TypeError) → 1.3.2 고정
Authlib==1.3.2

# ── 집계·정산 (2026-07-31 추가: 쓰고 있는데 빠져 있었다) ──────────────
# 대용량 parquet 집계의 핵심. build_photoism_agg·photoism_ingest·정산 모듈 전부
# 이걸로 돈다. 없으면 포토이즘 대시보드와 정산서가 통째로 ImportError.
duckdb>=0.10.0
# parquet 읽기/쓰기(딕셔너리 인코딩 포함). pandas 가 끌어오지만 직접 import 하는
# 파일이 많아 명시한다.
pyarrow>=14.0.0

# ── 스케줄러·환율 ────────────────────────────────────────────────
# scheduler.py 의 매일/매주 등록에 쓴다.
schedule>=1.2.0
# update_rates 의 공휴일 보정(환율 기준일). 없으면 공휴일을 조용히 무시해
# 휴장일에 엉뚱한 기준일을 잡는다.
holidays>=0.40
